<%= form_for(@game_role) do |f| %>
  <% if @game_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_role.errors.count, "error") %> prohibited this game_role from being saved:</h2>

      <ul>
      <% @game_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.text_field :game_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
