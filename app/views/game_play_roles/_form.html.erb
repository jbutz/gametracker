<%= form_for(@game_play_role) do |f| %>
  <% if @game_play_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_play_role.errors.count, "error") %> prohibited this game_play_role from being saved:</h2>

      <ul>
      <% @game_play_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_play_id %><br>
    <%= f.text_field :game_play_id %>
  </div>
  <div class="field">
    <%= f.label :game_role_id %><br>
    <%= f.text_field :game_role_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
